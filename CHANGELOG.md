# 변경 이력 (개발자용)

이 프로젝트의 기술적 변경 사항을 버전별로 기록합니다.
사용자 안내용 요약은 [RELEASE_NOTES.md](RELEASE_NOTES.md)를 참고하세요.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르며,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

## [Unreleased]

## [0.5.1] - 2026-08-13

### 변경
- 운동 이름 중복을 폼에서도 막는다. 피커(`ExercisePicker`)는 검색어로 만들 때 이미
  같은 이름을 막고 있었는데 운동 추가/수정 폼만 통과시켜, 같은 운동이 둘로 갈리면
  최고 기록과 향상 추적이 각각 반쪽이 됐다.
  - 판정은 `normalizeExerciseName`(공백 전부 제거 + 소문자화)으로 통일한다.
    「데드리프트」와 「데드 리프트」, 「Lat Pulldown」과 「lat pulldown」은 같은
    운동인데 띄어쓰기도 대소문자도 사람마다, 그날그날 다르다.
    저장은 사용자가 적은 그대로 두고 비교할 때만 정규화한다.
  - 수정 중인 자기 자신은 제외한다. 아니면 이름을 안 바꾼 수정이 통째로 막힌다.
- 운동 검색도 같은 규칙을 쓴다. 「데드 리프트」로 쳤을 때 「데드리프트」가 안 나오면,
  만들기는 중복이라 막혀 있어서 아무것도 못 하는 막다른 화면이 된다. 피커와 운동
  목록 두 검색창을 함께 맞췄다.

### 추가
- 렌더가 통째로 실패했을 때의 안내 화면(`ErrorBoundary`). 예외 하나로 화면이 비면
  설정 > 데이터 관리로 들어갈 수 없어 백업조차 뽑지 못했다 — 기록이 이 기기에만
  있는 앱에서는 그대로 유실 위험이다. 그래서 이 화면이 백업 내려받기를 직접 제공한다.
  - `ToastProvider`가 터지는 경우까지 잡도록 가장 바깥(`main.tsx`)에 둔다.
  - DB 자체가 깨져 백업까지 실패하면 실패했다고 말한다(성공한 척하지 않는다).
  - 「다시 시도」는 상태만 비우면 같은 예외가 곧바로 다시 나므로 `location.reload()`로
    통째로 새로 띄운다.

### 수정
- 데이터 읽기 실패가 「불러오는 중...」에서 영영 멈추던 문제 (15개 페이지).
  `useEffect(() => { load() })` 형태라 `load()` 안에서 예외가 나면 로딩 플래그를
  세우는 마지막 줄에 도달하지 못했고, 거부된 프라미스는 아무도 받지 않았다.
  저장·삭제 실패는 토스트로 드러내면서 읽기 실패만 조용했던 셈이다.
  - `useLoader` 훅으로 호출부에서 받아 `LoadError`(다시 시도 포함)를 보여준다.
    에러 바운더리로는 못 잡는다 — 렌더가 아니라 비동기 거부라 바운더리를 지나간다.
  - 홈·목록은 실패를 「기록이 없습니다」로 둘러대지 않는다(없는 것과 못 읽은 것은 다르다).
  - 설정 화면만 예외로 실패를 삼킨다. 백업 안내는 부가 정보인데 이걸 못 읽었다고
    메뉴를 에러로 덮으면 정작 복구에 필요한 「데이터 관리」로 들어갈 수 없다.
- 망가진 초안 때문에 기록 추가 화면이 잠기던 문제. 백업 복원은 `appConfig` 행을 파일에
  있는 그대로 넣기 때문에 손상된 초안이 들어올 수 있고, 초안을 읽는 쪽이
  `form.exercises.length`를 부르다 예외가 났다. 초안을 지울 UI가 없어 전체 초기화
  말고는 빠져나갈 방법이 없었다. `isDraftFormShape`로 모양을 확인해 어긋나면 없는
  것으로 보고 정리한다.
- 백업 파일 이름이 UTC 기준이라 한국 오전 9시 전에 백업하면 어제 날짜로 찍히던 문제.
  앱의 다른 날짜는 전부 로컬 기준인데 여기만 `toISOString()`이었다.
- `ExerciseFormPage`도 읽기 실패 시 「불러오는 중...」에 멈추지 않는다. 호출 형태가
  `if (isEdit) load()`로 달라 앞선 15개 페이지 정리에서 빠져 있었다.

## [0.5.0] - 2026-08-12

### 추가
- 작성 중이던 기록 복구. 헬스장에서 세트를 채우다 전화가 오거나 앱이 내려가면 그대로
  날아갔다. 입력 내용을 계속 임시 보관했다가, 다시 기록 추가 화면에 들어오면
  `이어쓰기 / 새로 시작`을 묻는다.
  - 저장 위치는 `appConfig`(키-값). 초안 하나 때문에 IndexedDB 버전을 올릴 이유가 없다.
  - 초안은 하나만 유지하고, 저장 완료·`새로 시작` 시 삭제. 7일이 지나면 조용히 버린다
    (그 이상 묵은 초안은 사용자도 기억하지 못한다).
  - 아무것도 안 적은 폼은 초안으로 치지 않는다(화면만 열었다 나간 경우).
  - 기록을 "추가"하는 경로는 전부 대상이다(빈 폼·복제·루틴으로 시작). 셋 다 `create`를
    호출하는 신규 작성이라 같게 다룬다. 수정만 제외 — 이미 저장된 기록이 있어 원본과
    초안 중 무엇을 보여줄지 애매해진다.
  - 손대지 않았으면 초안을 만들지 않는다. 루틴으로 시작했다가 그냥 나간 경우 프리필된
    내용이 초안으로 남아 다음에 뜬금없이 물어보는 걸 막는다.
  - 복구 다이얼로그는 배경 클릭·Esc로 닫히지 않는다(`ConfirmDialog`에 `dismissible` 추가).
    실수로 닫아 초안을 잃는 일을 막기 위함.
  - 나가기 안내도 함께 맞췄다. 초안이 남는데 "닫으면 변경사항이 사라집니다"라고 경고하면
    앱이 서로 다른 말을 하는 셈이라, 새 기록에서는 경고 대신 `임시 저장했습니다`로 알리고
    바로 나간다. 초안을 남기지 않는 수정만 기존 경고를 유지한다.
    나가는 순간 즉시 저장하므로 디바운스(500ms) 전에 나가도 내용이 남는다.
  - 화면이 가려지거나(`visibilitychange`) 페이지가 떠날 때(`pagehide`)도 즉시 저장한다.
    디바운스만 두면 정작 이 기능이 막으려는 순간(전화가 와서 앱이 백그라운드로 감)에
    마지막 입력이 날아간다. 다만 입력 직후 수백 ms 안에 앱이 강제 종료되면 IndexedDB
    쓰기가 끝나지 못해 마지막 입력은 유실될 수 있다(동기 저장소가 아니라서 생기는 한계).
- 백업 안내. 데이터가 전부 브라우저(IndexedDB)에만 있어 백업을 안 하면 복구할 방법이 없는데,
  사용자가 스스로 기억해서 누르길 기대하고 있었다. 마지막 백업 시각을 `appConfig.lastBackupAt`에
  기록하고, 설정 > 데이터 관리 항목에 상태(`오늘 백업함` / `마지막 백업 N일 전` /
  `아직 백업한 적 없음`)를 보여준다. 14일이 지났거나 한 번도 안 했으면 점 + 강조색으로 안내.
  - 데이터가 하나도 없으면 안내하지 않는다(빈 앱에 잔소리하지 않도록).
  - 판단은 `utils/backupReminder.ts`의 순수 함수로 분리해 기준을 한 곳에 뒀다.

- 수업 상세에 회원 진척 표시. 같은 회원의 직전 완료 수업과 비교해 운동명 옆에
  `최고 X ▲ 지난 Y`를 노출한다. 개인 모드 기록 상세에는 있었지만 트레이너 모드에는
  없어, 회원 진척을 앱에서 볼 방법이 아예 없었다.
  - 기록/수업이 같은 로직을 복붙하지 않도록 공용화: `utils/prevBest.ts`(exercises/routine
    필드명 차이를 `items`로 흡수), `components/BestProgress.tsx`(배지 마크업).

### 수정
- 쓰기 실패가 조용히 넘어가거나 성공으로 표시되던 문제(6곳). 실패했는데 "저장/삭제되었습니다"가
  떠서 사용자가 처리된 줄 알게 되는 경우가 대부분이었다. 0.1.1에서 폼 저장에 대해 같은 문제를
  고쳤지만 삭제·루틴 저장·인라인 생성은 빠져 있었다.
  - 거짓 성공 토스트: 루틴으로 저장, 루틴 삭제, 회원 삭제, 기록 삭제, 수업 삭제
  - 조용한 실패: `ExercisePicker`의 인라인 운동 생성이 `try/finally`만 있고 `catch`가 없어,
    실패하면 스피너만 꺼지고 아무 안내 없이 끝났다(호출하는 3개 폼도 unhandled rejection).
    picker에서 잡도록 해 세 폼이 함께 해결된다.
  - 사진을 base64로 IndexedDB에 저장하는 앱이라 용량 초과·프라이빗 모드 등에서 실제로 발생 가능.
  - 각 경로에 실패 토스트가 뜨고 데이터가 남아 있는지 확인하는 테스트 6개 추가.

- 목록에서 필터를 걸고 상세로 갔다 뒤로 오면 필터가 풀리던 문제. 검색어·필터를 `useState`로
  들고 있어 목록이 언마운트되면 초기값으로 돌아갔다. `utils/useFilterParams.ts`로 URL 쿼리에
  담아 뒤로가기가 복원하게 했다. 운동(검색·부위·장비) / 루틴(부위) / 회원(검색·수업종료) 적용.
  - `replace`로 갱신해 타이핑·칩 클릭이 히스토리를 더럽히지 않게 하고,
    `preventScrollReset`으로 필터 변경 시 목록이 맨 위로 튀지 않게 했다.
  - 검색어는 URL만 바라보면 빠르게 칠 때 URL 갱신이 한 박자 늦어 글자가 씹힌다
    (`데드리` → `리`). `useUrlBackedText`로 입력값은 로컬 상태에 두고 URL과 양방향 동기화.
    이 문제는 미출시 구간에서 생겼다 잡혔으므로 사용자에게는 노출된 적이 없다.
- 토스트가 긴 문구에서 불필요하게 줄바꿈되던 문제. `left: 50%`로 중앙을 잡으면 고정 요소의
  가용 폭이 화면의 오른쪽 절반(375px 화면에서 약 188px)으로 줄어, `max-width`가 넉넉해도
  그보다 긴 문구는 무조건 두 줄이 됐다. 좌우 여백 + `margin-inline: auto`로 센터링하도록 변경.
  「작성 중인 내용을 임시 저장했습니다」·「저장 실패: …」 같은 기존 문구들도 함께 한 줄이 된다.
- 긴 운동명이 편집 카드의 액션 영역(↑↓ 묶기 ✕)을 밀어 「묶기」가 "묶 / 기" 두 줄로
  쪼개지던 문제. 슈퍼세트 기능에서 텍스트 버튼을 추가하며 생겼다(아이콘만 있을 때는 안 보였다).
  `flex-shrink: 0` + `white-space: nowrap` + 이름에 `min-width: 0`.
- react-router 7.15.1 → 7.18.2. 이 앱에 해당되는 오픈 리다이렉트(`<Link>`/`useNavigate`
  백슬래시)와 라우트 매칭 DoS가 막힌다. 남는 1건(RSC Mode CSRF)은 `BrowserRouter`만 쓰는
  순수 클라이언트 SPA라 해당 없음. `audit fix --force`는 7.11.0으로 되돌리는 다운그레이드라
  적용하지 않았다.

### 내부
- 리포지토리 테스트 보강: `routineLogs`(복합 인덱스 커서 포함)·`routineTemplates`.
  다른 리포지토리에는 있었으나 이 둘만 비어 있었다.
- 백업 왕복 테스트 추가. `exportBackup`은 그동안 한 줄도 덮이지 않아 내보내기에서 필드가
  빠져도 알 수 없었다. 신규 필드(`groupId`·`assisted`) 보존을 잠가둔다.
- 간헐 실패하던 토스트 확인 4곳을 `findByRole`로 교체(async 동작 뒤 동기 조회 문제).
- 데모 데이터에 보조 무게·슈퍼세트 반영(운영 환경에서는 노출되지 않는 검증용 기능).
- 필터 유지 e2e 추가. 유닛(MemoryRouter)으로는 진짜 뒤로가기도 연속 타이핑도 재현할 수 없다.

## [0.4.0] - 2026-08-07

### 변경
- 1시간 이상 기록의 시간 표기를 `h:mm:ss`로. 분으로만 적던 탓에 9시간짜리 세션이
  `540:15`로 보여 읽기 어려웠다(`9:00:15`). 1시간 미만은 기존 `m:ss` 그대로.
  `formatDuration` 한 곳만 바꿔 세트 줄·기록 칩·최고 기록 배지·공유 이미지에 모두 반영된다.

### 추가
- `거리 + 시간` 운동의 기록 비교에 시간을 반영. 그동안 거리 최대값만 보고 시간은 아예
  계산에 들어가지 않아, 같은 5km를 더 빨리 뛰어도 향상으로 잡히지 않았다.
  **거리 우선, 같은 거리면 더 빠른 시간**이 나은 기록이다(더 멀리 뛰면 페이스가 느려져도 ▲).
  - 배지 표시도 `최장 5km` → `최장 5km 24:30`으로 시간을 함께 노출.
  - 값 하나로는 (거리, 시간) 두 축을 표현할 수 없어 기록 비교를 세트 단위로 바꿨다.
    `bestValue`/`formatBest`/`isImproved` → `bestSet`/`formatBestSet`/`isImprovedSet`.
  - `bestSetLabel`을 `constants.ts`에서 `utils/setStats.ts`로 이동(비교 로직과 같은 곳에 두고,
    `setStats`가 `formatDuration`을 가져오는 것과 맞물린 순환 참조를 피하려고).
  - 같은 거리에서 시간이 비어 있으면(0) 기록된 쪽이 낫다고 본다.
- 보조 무게(어시스트 머신·밴드 등) 지원. 운동 추가/수정에서 `보조 무게`를 켜면 그 운동은
  보조가 **적을수록** 잘한 것으로 계산한다 — 기록 배지가 `최고 40kg` 대신 `보조 30kg`(최소값)이
  되고, 이전 기록 대비 ▲▼ 방향도 뒤집힌다(보조가 줄면 ▲).
  - `Exercise.assisted?`(옵셔널) 추가 — 측정 방식(metric)과 독립이라 기록이 쌓인 뒤에도 켜고 끌
    수 있다(metric은 사용 중이면 잠김). 마이그레이션 없음.
  - 비워둔 세트의 `0`이 "보조 0kg"으로 최고 기록이 되는 것을 막기 위해 어시스트에서는 0을
    미입력으로 보고 최소 계산에서 제외한다(전부 0이면 기록 없음).
  - `무게 × 횟수`가 아닌 측정 방식으로 바꾸면 보조 무게 설정은 자동 해제된다.
- 슈퍼세트: 두 개 이상 운동을 한 세트(라운드)로 묶어 기록. 운동 헤더의 `묶기`로 바로 위
  운동과 묶고 `해제`로 풀며, 묶인 상태에서는 개별 `세트 추가`가 `라운드 추가`로 바뀌어
  멤버 전원에게 세트가 함께 늘고/줄어든다. 묶음은 ↑↓로 통째로 이동(찢어지지 않음),
  묶음 안에서는 ↑↓가 묶음 내 순서만 바꾼다. 기록·루틴·수업 편집에 모두 적용.
  - 데이터는 평면 `RoutineExercise[]`를 유지하고 `groupId?`만 추가 — 연속된 같은 groupId를
    한 묶음으로 해석(`utils/routineGroups.ts`). 평면으로 읽는 기존 코드(운동별 기록,
    삭제 가드, 백업 등)는 그대로 동작하고 IndexedDB 스키마 변경·마이그레이션이 없다.
  - 기록 복제·루틴으로 저장·루틴으로 기록 시작에서 `RoutineExercise`를 재조립하며
    `groupId`가 유실되던 지점을 `...r` 복사로 수정.
  - 이미지로 공유(기록·수업)에도 묶음 반영: 「슈퍼세트」 배지 + 라운드 수 머리줄, 멤버
    들여쓰기, 묶음 범위를 표시하는 왼쪽 세로선. 들여쓴 만큼 세트 줄바꿈 가용 폭도 좁혀
    계산한다.
- 운동 상세 사진을 꾹 누르면(길게 누르기) 원본 비율 그대로 전체 사진을 팝업(라이트박스)으로
  확인. 캐러셀은 `object-fit: cover`로 잘려 보이므로, 비율이 안 맞는 사진도 원본을 볼 수 있게
  함. 짧은 탭이나 캐러셀 스와이프와 충돌하지 않도록 450ms 임계값 + 이동 거리 감지로 구분
  (`PhotoLightbox`). 사진이 여러 장이면 팝업 안에서도 좌우 스와이프로 다른 사진 원본을
  이어서 확인 가능(점 인디케이터 포함).

## [0.3.0] - 2026-06-22

### 추가
- 운동 기록·수업 상세에서 이미지로 공유(`이미지로 공유`). 제목 + 부위 태그 + 운동별
  세트(인라인, 무게 숫자만 브랜드 컬러 강조) + 메모를 캔버스 PNG로 생성, Web Share API
  미지원 시 다운로드. 공용 `generateWorkoutShareImage`로 기록/수업 동일 형태,
  `최고` 계산은 `utils/setStats.ts`로 공용화. 수업 상세 헤더는 케밥(수정/이미지로 공유)으로 통일.
- 운동 목록에서 새 운동을 만들고 돌아오면 해당 운동으로 자동 스크롤(생성 id를 네비게이션
  state로 전달, 목록에서 `[data-ex-id]`로 찾아 가운데 정렬).
- 기록/수업/루틴 편집(`RoutineEditor`)에서 운동 순서 변경(↑/↓ 버튼). 첫/마지막은 해당 방향 비활성.

## [0.2.1] - 2026-06-20

### 수정
- 운동 상세 collapsing 헤더에서 사진 가로 스와이프가 안 되던 문제. 스크롤 오버레이
  (`exdetail__scroll`)가 사진 위를 덮어 캐러셀이 터치를 못 받던 것을 `pointer-events`로
  사진 영역만 통과시켜 해결(제목/본문은 그대로 스크롤).

## [0.2.0] - 2026-06-20

### 추가
- 설정 > 업데이트 내역(`/settings/updates`) 화면. `RELEASE_NOTES.md?raw`를 파싱해
  버전별 변경 사항 전체를 스크롤로 노출(외부 의존성 0). 버전 표시는 기존 위치 유지.
- 운동 선택(picker) 화면에서 새 운동 인라인 생성. 검색어를 이름으로 사용하고,
  측정 방식(metric)을 선택해 생성. 생성 후 해당 운동으로 자동 스크롤.
- 빈 운동 카탈로그에서도 picker를 열 수 있도록 `onCreateExercise` 제공 시 활성화.
- 사진 3장 이상인 운동을 이미지 공유할 때, 첫 2장만 포함된다는 확인 다이얼로그 노출.

### 변경
- 기록 상세 헤더 개선: 제목 + 부위 태그(기록 내 각 운동의 첫 카테고리만, 중복 제거)를
  한 줄에 두고 날짜·상태와 함께 상단 sticky 고정. 운동별로 `최고 X`(brand 배지) +
  직전 완료 기록 대비 `▲/▼ 지난 Y` 표시.
- 기록·루틴·수업 상세의 읽기 전용 운동/세트 목록을 공용 `RoutineReadonly` 컴포넌트로 통합
  (그동안 세 페이지가 동일 마크업을 복붙). 세트 값은 숫자 강조 + 단위(kg·×·회) 약화로
  대비를 줘서 가독성 개선(`renderSetValue`, `routine-readonly--emph`).
- 운동 상세 화면에 collapsing 헤더 적용: 스크롤하면 사진이 줄고(`heroH = 풀높이 − 스크롤량`)
  제목이 사진 하단 모서리를 타고 올라와, 사진이 완전히 접히면 상단(바 아래)에 sticky 고정.
  사진 풀높이는 `window.innerWidth × 3/4`로 계산.
- 운동 상세 "최근 기록"과 운동 기록 전체(`/exercises/:id/history`) 표시 개선:
  세트를 칩 단위로 표시(축약 `formatSetShort`, 예 `77.5kg×10`), 날짜는 요일 없는
  `formatShortDate`로 칩과 같은 줄에 인라인 배치. 최근 기록은 3개까지만 노출(초과 시 더보기).
- 데이터 관리 화면의 "데이터 초기화"를 화면 하단에 구분선 + 작은 텍스트 버튼으로 약화
  (섹션 타이틀/경고문구 제거). 실수 방지는 기존 확인 다이얼로그가 담당.
- 데이터 복원 경고문구의 빨간 강조 제거 + 문구 완화.
- `btn--ghost` 기본 배경을 surface로 변경(페이지 배경 위 보조 버튼의 가시성 개선,
  가장 흔한 컨텍스트를 기본값으로). 다이얼로그(배경 surface) 안에서는
  `.dialog .btn--ghost`로 `--color-bg` 채움 오버라이드해 취소 버튼 대비 유지.
- 공유 이미지 레이아웃: 사진 2장은 좌우 분할. 좌측 컬럼을 사진 2장으로 카드
  위/아래/왼쪽 가장자리까지 풀블리드로 채우고(cover), 우측에 운동명·메타·설명·푸터 배치.
  3장 적층 시 세로로 과도하게 길어지던 문제를 해결하기 위해 공유 사진을 최대 2장으로 제한.

### 수정
- 캐러셀 페이지 점이 흰색 사진 위에서 보이지 않던 문제(scrim 추가).

## [0.1.1] - 2026-06-15

### 수정
- SPA 라우트 새로고침/딥링크 시 404 발생 문제. Vercel rewrite + Service Worker
  `navigateFallback: index.html` 적용.
- IndexedDB 데이터 유실 예방: `navigator.storage.persist()`로 영구 저장소 요청.
- 폼 저장 실패가 조용히 무시되던 문제: 저장 핸들러를 try/catch로 감싸 "저장 실패" 토스트 노출.
- Android 설치 불가 문제: PWA 설치 기준 충족을 위해 PNG 아이콘(192/512/maskable) 추가.

## [0.1.0] - 2026-06-03

### 추가
- 개인 모드(트레이너/개인 겸용).
- 운동별 측정 방식(무게·횟수 / 횟수 / 시간 / 거리+시간).
- 운동 정보 이미지 공유(Canvas 기반 PNG 생성).
